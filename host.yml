---
all:
  vars:
    ansible_user: admin
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

prod:
  hosts:
    client1:
      ansible_host: 10.0.2.4
      instance_1:
        name: ENSUP
        port: 8080
        site_name: webapp
        title: MyWebApp
        host_ip: 10.0.2.4